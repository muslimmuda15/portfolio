{
  "name": "ai-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bash -c 'source venv/bin/activate && uvicorn main:app --reload --port 8000'",
    "start": "bash -c 'source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000'",
    "setup": "bash -c 'python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt'"
  },
  "description": "Python FastAPI AI backend for portfolio"
}
